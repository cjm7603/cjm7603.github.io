import styles from "./About.module.css"

export default function About() {
    return(
        <>
        <div id="aboutSection">
            <p1 className={styles.about}>This is the about section!</p1>
        </div>
        </>
    );
}